# Профиль по умолчанию в образе: выключен.
#
# Без доставленного контроллером контракта проверять нечего -- описания API у
# процесса нет и взяться ему неоткуда. Выключенный профиль честнее пустого:
# в аудите видно JSON_PROFILE_OFF, то есть "маршрут не проверяется", а не
# тишина, которую легко принять за "проверено и сошлось".
#
# Боевой default приезжает поколением из KV (policy/json) и подменяет этот
# каталог целиком -- см. docs/inspector-config-distribution.md.

mode: off
description: "Пустой профиль образа: контракт приезжает из контроллера"
